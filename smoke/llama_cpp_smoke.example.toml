# Placeholder Ember extraction config for a future llama.cpp-backed smoke test.
# This file is intentionally non-local: replace placeholder paths in a private copy.
# Do not commit real machine-specific model or binary paths.

run_id = "paper1-llama-cpp-smoke"
model_path = "/path/to/model.gguf"
architecture = "llama"
backend = "llama-cpp-external"
prompt_template = "{prompt}"
input_jsonl_path = "sarf-atlas/smoke/prompts.small.jsonl"
output_dir = "runs"
layers = []
token_position = "prompt_final"
sample_id_field = "id"
word_field = "word"
batch_size = 1
dtype = "f32"
output_format = "npy"
prompt_hashes_only = false
write_logits = false
resume = false
record_model_sha256 = false
llama_cpp_binary = "/path/to/llama-ember-extract"

[run_metadata]
workspace = "sarf-atlas"
paper = "paper1-leakage-aware-probing"
purpose = "local llama.cpp external backend smoke scaffold"
dry_run_default = true
no_generation = true
no_logits = true
no_hidden_states = true
not_research_output = true
